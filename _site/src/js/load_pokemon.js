async function loadPokemon(url) {
    fetch(pokemonUrl).then((response) => {
        // const name = response.name;
        // const id = response.order;
        // const weight = response.weight; //Weight in kg * 10
        // const frontSprite = response.sprites.front_default;
        // const backSprite = response.sprites.back_default;
        // const stats = { "hp": response.stats[0], "attack": response.stats[1], "defense": response.stats[2], "special-attack": response.stats[3], "special-defense": response.stats[4], "speed": response.stats[5] };
        // let types;
        // if (length(response.types) == 1) {
        //     types = { 1: response.types[0].type };
        // } else {
        //     types = { 1: response.types[0].type, 2: response.types[1].type }
        // }
        // const abilities = response.abilities;



    });
}
